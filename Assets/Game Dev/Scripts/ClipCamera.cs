using UnityEngine;

[RequireComponent(typeof(Camera))]
[AddComponentMenu("Game Dev Workshop/Clip Camera")]
public class ClipCamera : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
